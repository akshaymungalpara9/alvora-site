# Railway build configuration.
# Chromium is installed at build time by `npx playwright install --with-deps chromium`
# (part of pnpm build). That step downloads Playwright's pinned Chromium and installs
# all required system libraries via apt, which is more reliable than the `chromium-browser`
# apt package (a snap meta-package on Ubuntu 22.04 that cannot run inside containers).
